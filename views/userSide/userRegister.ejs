<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register an account</title>
    <!-- <link rel="shortcut icon" href="#" type="image/x-icon" /> -->
    <!-- Bootstrap css cdn link -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <!-- end of Bootstrap css cdn link -->
    <!-- Style css -->
    <link rel="stylesheet" href="/userSide/css/registerNewAccount.css" />
    <!-- end of Style css -->
  </head>
  <body>
    <!-- header -->
    <%- include('../includes/userSide/header') %>
    <!-- end of header -->
    
    <!-- main content -->
    <main>
      <section>
        <div class="container-fluid">
          <div class="row">
            <div
              class="col-12 m-0 loginsection d-flex align-items-center justify-content-center"
            >
              <div class="loginbox">
                <div class="headingLogin d-inline-block">
                  <p>Register</p>
                </div>
                <div class="inputForm d-inline-block">
                  <form action="/userRegister" method="post">
                    <label class="d-block" for="fullName">Fullname</label>
                    <input
                      class="d-block <%= errMesg?.fName?'errDiv':'' %>"
                      type="text"
                      name="fullName"
                      value="<%= userInfo?.fName?userInfo.fName:'' %>"
                    />
                    <p class="err <%= errMesg?.fName?'':'d-none' %>"><%= errMesg?.fName %></p>
                    <label class="d-block" for="email">Email</label>
                    <input
                      class="d-block <%= errMesg?.email?'errDiv':'' %>"
                      type="text"
                      name="email"
                      value="<%= userInfo?.email?userInfo.email:'' %>"
                    />
                    <p class="err <%= errMesg?.email?'':'d-none' %>"><%= errMesg?.email %></p>
                    <label class="d-block" for="phoneNumber">Phone</label>
                    <input
                      class="d-block <%= errMesg?.phone?'errDiv':'' %>"
                      type="number"
                      name="phoneNumber"
                      value="<%= userInfo?.phone?userInfo.phone:'' %>"
                    />
                    <p class="err <%= errMesg?.phone?'':'d-none' %>"><%= errMesg?.phone %></p>
                    <label class="d-block" for="password">Password</label>
                    <input
                      class="d-block <%= (errMesg?.pass || errMesg?.bothPass)?'errDiv':'' %>"
                      type="password"
                      name="password"
                    />
                    <p class="err <%= errMesg?.pass?'':'d-none' %>"><%= errMesg?.pass %></p>
                    <label class="d-block" for="confirmPassword"
                      >Confirm Password</label
                    >
                    <input
                      class="d-block <%= (errMesg?.conPass || errMesg?.bothPass)?'errDiv':'' %>"
                      type="password"
                      name="confirmPassword"
                    />
                    <p class="err <%= (errMesg?.conPass || errMesg?.bothPass)?'':'d-none' %>"><%= errMesg?.conPass || errMesg?.bothPass %></p>
                    <div class="text-center m-0">
                      <button class="loginBtn" type="submit">
                        Create Account
                      </button>
                    </div>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
    <!-- end of main content -->

    <!-- footer -->
    <%- include('../includes/userSide/footer') %>
    <!-- end of footer -->

    <!-- custom js -->
    <script src="/userside/js/registerNewAccount.js"></script>
    <!-- end of custom js -->
    <!-- fontawesome -->
    <script
      src="https://kit.fontawesome.com/23d1247997.js"
      crossorigin="anonymous"
    ></script>
    <!-- end of fontawesome -->
    <!-- Bootstrap script file -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
    <!--end of Bootstrap script file -->
  </body>
</html>
